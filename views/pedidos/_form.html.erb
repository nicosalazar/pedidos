<%= form_with(model: pedido, local: true) do |form| %>
  <% if pedido.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pedido.errors.count, "error") %> prohibited this pedido from being saved:</h2>

      <ul>
      <% pedido.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :fecha_despacho %>
    <%= form.datetime_select :fecha_despacho, id: :pedido_fecha_despacho %>
  </div>

  <div class="field">
    <%= form.label :observacion %>
    <%= form.text_area :observacion, id: :pedido_observacion %>
  </div>

  <div class="field">
    <%= form.label :num_pedido %>
    <%= form.number_field :num_pedido, id: :pedido_num_pedido %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
